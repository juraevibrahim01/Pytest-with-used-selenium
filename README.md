```sh

# 🚀 Автоматизация тестирования с Pytest и Selenium

Этот проект создан для автоматизации тестирования веб-приложений и API.  
Используются следующие технологии:

- **Python** — язык программирования  
- **Selenium** — автоматизация браузера  
- **Pytest** — фреймворк для запуска тестов  
- **Requests** — тестирование API  
- **venv** — виртуальное окружение Python  

Проект позволяет быстро создавать UI- и API-тесты, запускать их и получать отчёты.

---

## ⚙️ Виртуальное окружение (venv)

Виртуальное окружение Python позволяет изолировать зависимости проекта.  
Каждый человек, клонируя репозиторий, может установить все необходимые библиотеки без конфликтов версий.

### 🧩 Шаг 1. Создание окружения

    python -m venv venv 



Эта команда создаст папку venv с изолированным окружением Python.

## Шаг 2. Активация окружения
Windows (PowerShell):

`.\venv\Scripts\Activate.ps1`
Windows (cmd):

`.\venv\Scripts\activate.bat`
Linux / macOS:

`source venv/bin/activate`
После активации вы увидите, что в начале строки терминала появилось (venv).

##  Шаг 3. Установка зависимостей
`pip install -r requirements.txt`
В requirements.txt перечислены все библиотеки, необходимые для проекта (Selenium, Pytest, Requests и др.).

### Как работает проект
Активируйте виртуальное окружение.

Запустите тесты командой:
`pytest`

Для получения более детального отчёта используйте:
`pytest -v`

или с генерацией HTML-отчёта:
`pytest --html=report.html`
```